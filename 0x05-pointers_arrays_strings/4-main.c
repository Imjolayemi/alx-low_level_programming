#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always Zero(0) Success
 */

int main(void)
{
	char *str;
	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
